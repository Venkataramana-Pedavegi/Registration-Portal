{
  "name": "college-event-registration-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for College Event Registration Management System",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
